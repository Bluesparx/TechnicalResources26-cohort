#include<iostream>
using namespace std;

int main(){
    int num, rev=0;
    cout<<"Enter number: ";
    cin>>num;
    do
    {
        rev=rev*10+num%10;
        num=num/10;
    }while (num%10!=0);
    cout<<rev;
    return 0;
} 
